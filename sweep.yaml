project: VAE(CRPS)
# program: main.py
program: inference.py
method: grid
metric:
  goal: minimize
  name: loss
parameters:
  num:
    values: [4, 5, 6, 7]
  # beta:
  #   values: [0.1, 1]
  # lr:
  #   values: [0.001, 0.01]